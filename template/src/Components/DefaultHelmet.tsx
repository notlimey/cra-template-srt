import { Helmet } from "react-helmet";

const DefaultHelmet = () => {
    return (
        <Helmet>
            <title>Welcome to react!</title>
        </Helmet>
    );
}

export default DefaultHelmet;